so today were gonna be creating a brand new C++ programming language called HSX yes HSX this language will be designed to run super fast, why because instead of mostly coding, it's like putting in commands in text, so here's how HSX works and I'll show you all the language you have to do and sybmols
so see this HSX: that's the first thing you add to each thing before you start programming so for example before you start coding with HSX you always do HSX: at the beggining that's required.
now you see this HSX: {GT} now GT is used instead of GET we do GT for getting something like a response, now you put these {} and than put GT inside of them.
now you see this *VALUE.SUM =1.5* that's like getting a sum value you do that so yeah the language is pretty simple but it's not just this were gonna go over all the things in this language so let's continue but VALUE.SUM is for like getting a sum of a number VALUE and it's required to have these * at the beggining and end and it's required to have a = sign and you cannot put a number like this 3 you have to for 3 put 3.5 it's required, so for example If I done *VALUE.SUM =3 now tell me is anything missing? 
now let's contiune for Conditons and loops it's pretty simple first you start with HSX: than you do !LP! LP stands for loop than you do HSX: !LP! now than for the loop you would after !LP! you'd do just like the VALUE.SUM you'd put :HSX !LP! =1.0 or like 3.5 so any number that you put in anything in hsx it always has to have a decimal, it cannot be 3 it has to be 3.0 
so now for while loops it's kinda of the same thing you do HSX: @WHL@ *VALUE.8 =7 so than tell me if that's right because you really didn't explain while loops that good.
now good job deepseek becuase this HSX: @WHL@ *VALUE.8 =7.0* was the right answer, but after were done defining all of these, you will code this C++ language but we will get more into that later right now this language is still being built.
now for conditions like if/else we would do C>Condition<C = F>If<F error E>Else>E =Print now do you see the pattern for condtions this is the pattern you do a Mathcing starting letter than do a > for the start and > for the end now here's what it would fully look like HSX: C>Condition<C = F>If>F error E>Else>E =Print
okay you know BTN right where instead of that we use BAIT for Button has a nice ring to it, now for button to make it unique we do +B_A_I_T+ yes exactly like that and than for all text in when you do text for anything it's ""Click Me"" so the full pattern would be HSX: +B_A_I_T+ ""Click Me ""
now for Input you'd think it'd be INP but instead it's INUT Yes INUT and to make it even more unique you do I_N_U_T and let's just be honest all things that have unique names have always gotta have this _ in between the letters but like I was saying {I_N_U_T} ""Enter Name"" so the full pattern would be {I_N_U_T} ""Enter Name""